## 평균 검정                   #################################################

# - 1. 일표본 평균 검정
# - 2. 독립표본 평균 검정
# - 3. 대응표본 평균 검정


# t분포     dt()          pt()          qt()          rt()

pt(3.58, df=20-1, lower.tail = FALSE) # 자유도가 19인 t분포에서 3.58 값이 나올 확률 ?


qt(0.025, df=20-1, lower.tail = FALSE) # t분포에서 오른쪽 2.5%에 위치한 값은?
